<a href="/admin">Back</a>
<h2>All Users</h2>
<div>
    <p>we have <%= users.length %> users</p>
    <% users.forEach(user => { %>
        <div>
            <h3><%= user.name %></h3>
        </div>
    <% }) %>
</div>